export const CONTRACT_APPLICATION_STATUS = Object.freeze({
    PENDING: "pending",       // Application submitted, not reviewed
    ACCEPTED: "accepted",     // Application accepted
    REJECTED: "rejected",     // Application rejected
    WITHDRAWN: "withdrawn",   // User withdrew application
});
